# Optional. Override storage location for browser profile / storage state / cache.
# Default: ~/.alza-cli/
# ALZA_CLI_HOME=/custom/path

# Throttle (seconds between HTTP requests). Default 2.5
# ALZA_CLI_THROTTLE=2.5

# User agent override (default: Chrome on macOS).
# ALZA_CLI_USER_AGENT=
